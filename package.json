{
  "name": "adriana-game",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "cap:sync": "nuxt generate && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "nuxt": "^4.0.0",
    "phaser": "^3.85.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
