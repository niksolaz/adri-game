import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.niksolaz.adriana',
  appName: 'Adriana e i Cuoricini',
  webDir: 'dist',
  // Landscape per il platformer
  plugins: {},
  android: {},
  ios: {}
}

export default config
